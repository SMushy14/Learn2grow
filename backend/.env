# The port your server will run on
PORT=5000

# Your MongoDB connection string
# (Replace this with your actual MongoDB Atlas URI if using cloud, or keep it local for now)
MONGO_URI=mongodb://localhost:27017/rwandan_learning_platform

# Secret key for generating JWT tokens later
JWT_SECRET=your_super_secret_key_here